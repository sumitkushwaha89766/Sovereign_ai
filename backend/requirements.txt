# Phase 1 dependencies (Plan.md §1 stack). Versions unpinned; pin via `pip freeze` after install.
fastapi
uvicorn
python-multipart
sqlalchemy
pydantic-settings
python-jose
bcrypt==4.1.2
chromadb
sentence-transformers
python-docx
openpyxl
python-pptx
pymupdf
pytesseract
pillow
# Phase 7 additions
httpx          # synchronous Ollama API calls in model_router.py
pyyaml         # model_registry.yaml parsing
lxml           # direct use in docgen_tool.py PPTX background fill (python-pptx transitive dep; explicit for clarity)
# Phase 9 additions (testing)
pytest         # unit + edge-case test suite (backend/tests/); manual_e2e.py reuses httpx
